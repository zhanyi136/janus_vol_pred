好的，给你规划一个检查顺序：

实盘代码阅读顺序
Step 1：数据入口 — predict_service.py 的 recv_loop
目标：搞清楚收到的消息长什么样，怎么过滤和缓存。

重点看：

Event=symbol 消息 → 怎么存 tick_size_cache
event=bookTicker 消息 → 怎么用 updateID 过滤，怎么存 latest_tick
Step 2：采样逻辑 — predict_service.py 的 sample_loop
目标：搞清楚每 20ms 做了什么。

重点看：

什么条件下会 continue（跳过）
tick_size 怎么传给 computer
tradeTime * 1000 的单位转换
三种输出状态（warmup / no_model / ok）
Step 3：特征计算 — realtime_feature.py
目标：搞清楚 mid_chg 怎么算，波动率怎么算。

重点看：

环形缓冲区的写入逻辑（_head, _count）
mid_tick = round(mid_price / tick_size, 1) 与批处理一致性
_rolling_std 的计算逻辑
预热期判断条件
Step 4：模型加载 — model_loader.py
目标：搞清楚模型从哪找，怎么热更新，怎么预测。

重点看：

find_model_dir 怎么往前找日期
check_and_reload 用 mtime 判断是否需要重载
predict 的输入输出（features dict → float）
Step 5：主入口初始化 — predict_service.py 的 __main__
目标：确认所有参数从 config 正确读取。

重点看：

results_dir 是否是 Path 类型
warmup_minutes 从 realtime 读还是 sampling 读
ZeroMQ bind/connect 方向是否正确
建议方式
逐步来，每次我们看一个 Step，你有不懂的就问我。从 Step 1 开始