# 伪代码
while True:
    new_tick = receive_tick()  # 接收新数据
    rolling_window.add(new_tick)
    
    features = compute_features(rolling_window)  # 计算 vol_1m, vol_5m 等
    X = np.array([[features[col] for col in feature_cols]])
    
    y_pred_qt = model.predict(X)
    y_pred = qt.inverse_transform(y_pred_qt.reshape(1, -1)).flatten()[0]
    
    print(f"预测波动率: {y_pred}")
